Swagger path http://localhost:8080/api#/

Api path http://localhost:8080

To run you need to have docker installed and running

npm run seed - seed DB with init data

npm run dev - run start to run the project
